language: java

dist: xenial

jdk: openjdk8

services:
  - docker
  - xvfb

addons:
  sonarcloud:
    organization: "amathebest-github"
    token:
      secure: "7355db91ee97013661a862b27a5047107c6a8d5f"

install: true

cache:
  directories:
    - $HOME/.m2
    - $HOME/.sonar/cache

script:
  - mvn -f com.project.apt.gestionale/pom.xml clean verify -Pjacoco sonar:sonar
